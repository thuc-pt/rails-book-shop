<div class="container">
  <div class="jumbotron">
    <h1 class="text-center">NHẬP KHO</h1>
    <br>
    <%if @books.empty? %>
      <h1>Chưa có sách mới</h1>
      <br>
      <%= link_to 'Thêm sách mới', new_book_path, class: 'btn btn-success btn-lg' %>
    <%else%>
      <%= render 'form', bookstore: @bookstore %>
    <% end %>
  </div>
</div>