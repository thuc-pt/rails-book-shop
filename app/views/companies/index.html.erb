<div class="container">
  <p class="text-center text-success"><%= notice %></p>
  <div class="row">
    <%=render 'admin/list' %>
    <div class="col-xl-9">
      <div class="jumbotron">
        <h1>Nhà cung cấp</h1>
        <br>
        <table class="table">
          <thead>
          <tr>
            <th>Name</th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <% @companies.each do |company| %>
            <tr>
              <td><%= company.name %></td>
              <td><%= link_to 'Edit', edit_company_path(company), class: 'btn btn-warning' %></td>
              <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
        <br>
        <%= link_to 'New Company', new_company_path, class: 'btn btn-success' %>
        <p><%=will_paginate @categories, class: 'justify-content-center' %></p>
      </div>
    </div>
  </div>
</div>