<div class="container">
  <p class="text-center text-success"><%= notice %></p>
  <div class="row">
    <%=render 'admin/list' %>
    <div class="col-xl-9">
      <div class="jumbotron">
        <h1>Ngôn ngữ sách</h1>
        <br>
        <table class="table">
          <thead>
          <tr>
            <th>Name</th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <% @languages.each do |language| %>
            <tr>
              <td><%= language.name %></td>
              <td><%= link_to 'Edit', edit_language_path(language), class: 'btn btn-warning' %></td>
              <td><%= link_to 'Destroy', language, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
        <br>
        <%= link_to 'New Language', new_language_path, class: 'btn btn-success' %>
        <p><%=will_paginate @languages, class: 'justify-content-center' %></p>
      </div>
    </div>
  </div>
</div>